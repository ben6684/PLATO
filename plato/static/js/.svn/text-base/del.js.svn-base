
// function del_source(id)
// {
// 	r=confirm("are you sure you want to delete this Source and all the media attached ?");
// 	if(r==true)
// 	{
// 		document.location.href="/util/del/s/"+id
// 	}
// }
function del_media(id)
{
	r=confirm("are you sure you want to delete this media ?");
	if(r==true)
	{
		document.location.href="/add_data/del/m/"+id
	}
}
function del_file(id)
{
	r=confirm("are you sure you want to delete this file (if there is only one file in the media, the media will be delete)?");
	if(r==true)
	{
		document.location.href="/add_data/del/f/"+id
	}
}

function del_tool(id)
{
	r=confirm("are you sure you want to delete this source code ?");
	if(r==true)
	{
		document.location.href="/add_app/del/a/"+id
	}
}

function del_page(id)
{
	r=confirm("are you sure you want to delete this publication ?");
	if(r==true)
	{
		document.location.href="/add_publi/del/p/"+id
	}
}

function quit_group(usr,id)
{
	r=confirm("are you sure you want to quit this group ?");
	if(r==true)
	{
		document.location.href="/add_group/"+usr+"/"+id+"/del";
	}
}